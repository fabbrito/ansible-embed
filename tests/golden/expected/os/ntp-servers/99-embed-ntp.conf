# Rendered by Ansible — do not edit on host.
# Source: fabbrito.embed collection — roles/os/templates/99-embed-ntp.conf.j2
[Time]
# NTP= appends; the empty one drops servers set by earlier files.
NTP=
NTP=0.pool.example 1.pool.example
