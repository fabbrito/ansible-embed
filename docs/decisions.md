# Decisions

Settled decisions about this collection, at repo altitude: what was chosen, and what would have to change for the choice
to stop being right. Backfilled as decisions come up, never all at once.

Repo altitude means no paths, no line numbers, no per-role detail — a decision that only makes sense beside one file
belongs in that file's comments, which move when it does.
