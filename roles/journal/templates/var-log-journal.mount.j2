# Rendered by Ansible — do not edit on host.
# Source: fabbrito.embed collection — roles/journal/templates/var-log-journal.mount.j2
[Unit]
Description=Journal on the storage volume
# The volume first: without the stick the mount fails and journald stays in
# /run, rather than writing to the card.
RequiresMountsFor={{ storage_path }}

[Mount]
What={{ storage_path }}/{{ journal_dir }}
Where=/var/log/journal
Type=none
Options=bind

[Install]
WantedBy=local-fs.target
